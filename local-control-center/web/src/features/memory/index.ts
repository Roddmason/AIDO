/**
 * Placeholder barrel for the memory feature. The Memory & Retrieval page currently
 * lives in `features/pages.tsx` (MemoryPage); this empty module reserves the folder
 * for when memory-specific UI is extracted here. The `export {}` keeps it a module.
 */
export {};
